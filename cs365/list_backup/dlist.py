# dlist.py
# Mitchell Wade
# March 31, 2015
# This program is the data structure for a dlist
# in python. There is also a driver in the tar
# file called DListDriver.py to test the program.

import list
import comparator
# import DListDriver
# import __init__

class list:

class DNode(list):

    def __init__(self, comparator, flink, blink):
        self.sentinel = Node
        self.comparator = comparator
        self.flink = flink
        self.blink = blink

class DList(list):

    flink = None
    blink = None

    # inserts the value before specified node
    def insertBefore(self, value, node):
        
        node = Node(data, None, None)
            if self.flink 

    # appends the value to the list
    def append(self, value):
    
    # remove the node from the list
    def erase(self, node):

    # a generator function that returns the next 
    # node in the list
    def find(self, value):

class DNode(list):

    def __init__(self, comparator, flink, blink):
        self.comparator = comparator
        self.flink = flink
        self.blink = blink
